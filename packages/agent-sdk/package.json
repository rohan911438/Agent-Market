{
  "name": "@rohankumar4179/agent-sdk",
  "version": "0.1.0",
  "type": "module",
  "description": "TypeScript client for autonomous agents calling AgentMarket's x402-metered APIs — automatic payment, budgets, retries, fallbacks, and usage tracking.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --max-warnings 0",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@rohankumar4179/shared-types": "^0.1.0",
    "@x402-avm/avm": "^2.6.1",
    "algosdk": "^3.6.0"
  }
}
